<%= form_for @registration do |f| %>
  <%= error_messages_for @registration if @registration.errors.length > 0 %>
  <%= f.label :name %>
  <%= f.text_field :name %> <br />
  <%= f.label :email %>
  <%= f.text_field :email %><br />
  <%= submit_tag %>
<% end %>
